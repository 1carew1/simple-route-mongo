version: "2"
services:
  web:
    build: .
    volumes:
      - ./:/app
    ports:
      - "8090:8090"
    links:
      - mongo
  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes_from:
      - mongodata
  mongodata:
    image: tianon/true
    volumes:
        - /data/db
